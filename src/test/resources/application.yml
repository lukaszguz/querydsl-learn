spring:
  datasource:
    username: sa
    password:
    url: jdbc:h2:mem:AZ;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    data: import.sql,import2.sql
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
